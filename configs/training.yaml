# Training configuration placeholder.
# TODO: define batch sizes, optimizers, and schedules in later phases.

training:
  batch_size: 32
  epochs: 1
  learning_rate: 0.0001
  optimizer: adam

preprocessing:
  image_size: 224
  audit_uses_augmentation: false
